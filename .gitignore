# 덱 스냅샷 — deck.html 을 렌더해서 만든 산출물. data.js + 매니페스트로 언제든 다시 만든다
pages/deck.snapshot.html

# 코드 블록 다크/라이트 비교 시안 — 판단 끝나면 지워도 된다
pages/_syntax-preview.html

# 제출용 덱 — 문서용(인쇄물)이라 공개하지 않는다. 로컬에만 둔다.
# SlideDeck.jsx · slides.css 는 _syntax-preview 가 함께 쓰고 사실을 담지 않아 유지.
pages/deck.html
pages/deck.snapshot.html
pages/deck/

# 제출용 덱의 PDF 출력물 — deck.html 과 같은 이유로 공개하지 않는다.
pages/deck.pdf
