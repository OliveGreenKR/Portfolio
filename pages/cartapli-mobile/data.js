// pages/cartapli-mobile/data.js
// ─────────────────────────────────────────────────────────────────────────────
// 이 파일이 이 페이지 문장의 유일한 원본이다. 중간 문서를 만들지 않는다.
// 상류 판단(사실 → 전략 → 구조)의 원본:
//   knowledge_base/projects/cartapli_mobile/research/portfolio-flow.md
//   §01 사실([FREEZE] · [CODE PAIRS] P1~P6 · [CODE EXCERPTS] C1~C6 · [MARKER SUM])
//   §02 전략(2026-08-15 개정)  ·  §03 구조(2026-08-15 재작성, 12절 계약)
//
// ── 절 계약 (§03 2026-08-15). 순서·개수를 여기서 바꾸지 않는다 ────────────────
//   S1 히어로 / S2 개요·경계 / S3 최종결과 / S4 지도 / S5 진단
//   S6 ①재사용 / S7 ②삭제 / S8 ③병합 / S9 ④Burst / S10 ⑤재측정 / S11 ⑥판정
//   S12 검증·한계
//   S5 이후의 순서는 커밋 날짜가 잠근다 —
//     385fd68(7/22) → 992c50a(7/23) → 792bb3a(7/24) → cafa4ae(7/24)
//     → f920035(7/24) → 0d73c39(8/12).  순서를 바꾸면 날짜가 어긋난다.
//   S6~S11 은 고정 리듬 — [기존→개선 그림] → [코드 쌍] → [설명 3줄]. 3줄을 넘기지 않는다.
//
// ── 수치 규칙 ────────────────────────────────────────────────────────────────
// 1. 시간·할당 수치가 나오는 자리마다 아래 한 문장을 붙인다. 문구를 바꾸지 않는다.
//      "Windows PC · Unity 에디터 PlayMode · 임의 접기 벤치(시드 12345) 16회차"
//    → 상수 CM_COND. 조건을 떼면 E3(동일 조건 전후 비교) 자격을 잃는다.
// 2. 구조 불변식(s8.table — 렌더 오브젝트·드로우콜·레이어·정점)에는 COND 를 붙이지 않는다.
//    플랫폼 무관이라 조건이 필요 없고, 붙이면 오히려 약해 보인다.
// 3. 측정 시점을 밝힌다 — s3 막대 · s8 표 = 2026-07-24 / 판정 정책 ① 상태.
//    s11 의 정책 비교 = 2026-08-12 / 세 방식을 따로 돌린 별도 하네스.
//    ⚠️ s11(⑥)을 s3 의 다섯 막대에 얹지 않는다. 단계가 아니다(§01 [FREEZE]).
// 4. §01 에 없는 수치를 쓰지 않는다.
//    ⚠️ 어긴 적이 있다 — 옛 문안이 "근거 자료에 없어"라고 적었는데 값은 있었다.
//       §01 인용이 두 줄 짧아 표를 못 실은 것이었다. 없다고 적기 전에 §01 을 의심한다.
// 5. **결론 수치**(마커 합 · 개선률 · 진단 배수)는 전부 Average 다.
//    ⚠️ 스파이크와 구간 대표값은 Median/Max 다 — s11 의 0.247/0.083(판정 최대) ·
//    38.5/45.9µs(구간 중앙값), s10 의 229,300/205,900(r13~16 중앙값), s9 의 할당 쌍.
//    그 자리는 성격을 문장에 밝힌다. "페이지에 Median 이 0개다" 라고 쓰지 마라 — 거짓이다.
//
// ── 문장 규칙 ────────────────────────────────────────────────────────────────
// 6. 주장 세기 ≤ 증거 급. E1(구조적 불변식)에서는 방향·불변식만 쓰고 "정도"는 못 쓴다.
//    금지 어휘: 크게 개선 · 성능 향상 · 최적화했다 · 효율적으로 · 획기적으로 ·
//               강력한 · 최적의 · 다양한 · 사용자 경험을 향상 · 많은 것을 배웠다
// 7. 측정 안 한 판단에는 "재지 않았다"를 붙인다.
// 8. ⚠️ 커스텀 생명주기 프레임워크에 **성능 주장을 붙이지 않는다** — s9 의 한 줄이 전부이고,
//    그 한 줄은 "Job 을 쓰려면 프레임 내 위치를 선언할 수단이 필요했다" 까지만이다(§02 확정 1번).
// 9. renderInline 은 **굵게** · `코드` · *기울임* 만. 중첩 금지, 목록기호·링크·취소선 미지원.
//    코드 쌍의 intro / result 는 renderInline 을 안 거친다 — 마크업을 쓰지 않는다.
// 10. 이모지는 ⚠️ 와 ✓ 둘뿐(DESIGN_SYSTEM.md).
//
// ── 금지 수치 ────────────────────────────────────────────────────────────────
//   정점 251 을 진단(s5) 자리에 쓰는 것 — S1-2 이후 값이다. 진단은 1,348.
//   회차 시간(FoldRoundTime) 전량 — 리포트 스스로 "발표 지표로 쓰지 말 것".
//   정렬 접기 벤치 수치 — 폐기된 벤치. "진단 당시 8배" 한 값만 s5 에 틀렸던 값으로 남긴다.
//   옛(로그 포함) 시리즈 — 전부 과소표시본. −70.0% 한 값만 s10 에 살린다.
//
// ── 뺀 것과 이유 (§02 REMOVE / DEFER 요약) ───────────────────────────────────
// REMOVE  커스텀 생명주기 프레임워크 서술 전량(목표에서 제외, 성능 수치 없음 E0~E1)
//         ⑥을 다섯 단계 막대에 얹는 것 · 각 방식 절의 구구절절(3줄 상한)
//         Median·Max 통계표 · 외부 발표 인용 · 발표자 실측치(12%/34%/70배)
//         SRP Batcher/Dynamic Batching/GPU Instancing 비교 · bench-overlay 절차 세부
// DEFER   외곽선 렌더링(측정 없음) · 배틀/Geo/Motion/Surface(근거 문서 없음)
//         기기 실측 · GPU 시간 · 오버드로우 — s12 한계로만 언급
//         커스텀 생명주기 프레임워크 — 컨텐츠·개발량이 쌓이면 별도 항목으로
// ─────────────────────────────────────────────────────────────────────────────

const CM_COND = 'Windows PC · Unity 에디터 PlayMode · 임의 접기 벤치(시드 12345) 16회차';

window.CM_DATA = {
  cond: CM_COND,

  /* ── S1 히어로 (Level 1) ─────────────────────────────────────────────── */
  meta: {
    eyebrow: 'PROJECT · PERFORMANCE',
    title: 'Cartapli Mobile',
    subtitle: '종이접기 시뮬레이션 — 모바일 이식과 리메이크',
    core: '어느 변경이 얼마를 벌었는지 갈라 말할 수 있게, 한 단계씩 고쳐 얼려 두고 같은 벤치로 다시 쟀다.',
    headline: {
      v: '0.643 → 0.040 ms',
      k: '접는 동안의 프레임당 마커 합 · S0 대비 −93.8%',
      href: '#result',
      hrefLabel: '어느 단계가 얼마를 벌었나 → 02',
    },
    pills: [
      { text: '2026-07 ~ 08 · 진행 중' },
      { text: 'Unity · Burst / Job' },   // sage 색면은 .cm-core 하나뿐이다 — accent 필을 쓰지 않는다
    ],
  },

  hero: {
    src: 'cartapli-mobile/assets/fold-manual.gif',
    w: 1308,
    h: 602,
    alt: '벤치 씬에서 종이를 수동으로 접는 장면. 회차를 거듭할수록 레이어가 쌓인다.',
    caption: '벤치 씬에서 손으로 접는 장면. 회차를 거듭할수록 조각이 갈라져 레이어가 쌓인다 — 이 페이지의 모든 수치가 **이 동작 하나의 비용**이다.',
  },

  /* ── S2 개요와 저작 경계 (Level 3) ───────────────────────────────────── */
  s2: {
    facts: [
      ['프로젝트', 'PC 원작 `Cartapli` 의 모바일 이식 + 리메이크. 퍼즐 게임이고 **개발 중**이다. 원작과는 **별개 레포**이고 커밋 122개가 전부 단독 작업이다.'],
      ['목표', '기존 로직을 모바일 환경에 맞게 고쳐 나가는 **학습·개발**이다. 그래서 *"DOTS 를 쓰면 빨라진다"* 가 아니라 **구조 개선으로 얼마, DOTS 로 추가 얼마**를 갈라 말하는 것을 목표로 잡았다. 여기서 재는 DOTS 는 **Burst 잡 하나**다 — ECS 로 갈아엎지 않았다.'],
      ['얼리기', '단계마다 그 시점 엔진을 브랜치로 얼려 둔다 — 태그 1개(`perf-s0`)와 브랜치 4개(`bench-s0` ~ `bench-s2-a`). 얼리는 것은 **엔진 로직**이고 벤치와 계측은 최신으로 통일한다. 그래서 한 달 뒤에도 **같은 자극으로 다시 잴 수 있다** — 09 가 실제로 그렇게 했다.'],
      ['용어', '**레이어** = 접어서 갈라진 종이 조각 한 겹. 접을 때마다 잘린 쪽이 늘어 회차를 거듭할수록 쌓인다 — 이 벤치의 16회차에서 337장이다. 조각 하나가 정점 서너 개짜리라, **기하 전체는 네 자리 정점에 불과한데 유니티 객체는 수백 개**가 된다 — 이 어긋남이 이 페이지의 출발점이다.'],
      ['프레임 두 종류', '접는 선을 **끄는 동안**이 보간 프레임이고, **놓는 한 프레임**이 확정이다. 끄는 동안은 종이 전체가 매 프레임 다시 만들어지고, 확정할 때만 조각이 실제로 갈라진다. 이 페이지의 모든 수치는 둘 중 어느 프레임의 비용인지로 갈린다.'],
      ['벤치', '정사각형 종이 한 장에 시드 12345 의 결정론 난수로 각도·비율·방향을 뽑아 **16회 접는다.** 대표 지표인 **"프레임당 마커 합"** 은 `Renderer.Sync` · `FoldOperation.Split` · `FoldOperation.Compose` 세 마커의 프레임당 Average 를 더한 값이고, **S0** 는 데이터·연산·뷰를 분리하는 리팩터까지만 마친 출발 상태다.'],
    ],
    mine: '가려진 레이어 판정 · 앞뒤 2메시 병합 렌더러 · Burst 잡 분할 파이프라인 · 오라클 테스트 · 측정 인프라. 지금 돌아가는 분할 커널도 원작 시절 내가 UV 대응으로 다시 쓴 것이고, 이 레포는 그것을 이식했다.',
    others: '점과 직선의 대칭으로 접기를 연출한다는 **아이디어와 최초 구현은 PoC 입안자의 것**이다. 원작 팀 레포의 최초 커밋에 이미 들어 있었다. 이 페이지가 다루는 것은 그 위에 올린 것들이다.',
  },

  /* ── S3 최종 결과 (Level 2) — §01 [MARKER SUM] ───────────────────────── */
  s3: {
    gist: '갈라 보니 구조를 바꾼 몫이 96.4%, Burst 잡으로 옮긴 몫이 3.6% 였다.',
    lead: '결론부터 놓는다. 아래 다섯 막대는 얼려 둔 브랜치를 **같은 벤치로 다시 돌려** 잰 값이다. 어떻게 다시 잴 수 있었고 왜 다시 쟀는지는 09 가, 각 단계가 무엇을 고친 것인지는 05~08 이 답한다.',
    chart: {
      unitNote: 'S0 = 100 기준',
      legend: [
        { group: 'base', label: '기준 (S0)' },
        { group: 'struct', label: '구조 개선' },
        { group: 'burst', label: 'Burst 잡' },
      ],
      bars: [
        { stage: 'S0', name: '리팩터까지만', pct: 100, delta: '기준', group: 'base' },
        { stage: 'S1-1', name: '① 안 바뀐 레이어는 다시 만들지 않는다', pct: 58.1, delta: '−41.9%', group: 'struct' },
        { stage: 'S1-2', name: '② 가려진 레이어를 지운다', pct: 25.6, delta: '−74.4%', group: 'struct' },
        { stage: 'S2-a', name: '③ 앞뒤 2메시로 병합한다', pct: 9.7, delta: '−90.3%', group: 'struct' },
        { stage: 'S2-b', name: '④ 분할을 Burst 잡 + 네이티브 버퍼로', pct: 6.2, delta: '−93.8%', group: 'burst' },
      ],
    },
    caption: '`Renderer.Sync` · `FoldOperation.Split` · `Compose` 세 마커의 프레임당 Average 합, S0 대비 · 0.643 → 0.040 ms. 줄어든 0.603 ms 중 **구조 개선이 0.581 ms(96.4%), Burst 잡이 0.022 ms(3.6%)** 다.',
    notes: [
      '⚠️ 이 다섯은 **2026-07-24 측정**이고, 그때 런타임의 가려짐 판정 방식은 10 에서 ①로 부르는 것이었다. 10 에서 채택한 방식은 그 뒤(2026-08-12)라 이 시리즈에 들어 있지 않다.',
      '⚠️ 이 막대는 **경과**다. 줄어든 값의 44.7% 가 ①의 몫인데 **그중 뷰 쪽 절반은 ③이 무효로 만들었다** — 05 가 그 이야기를 한다. ③·④ 값은 그 뷰 쪽이 죽은 뒤에 잰 것이라 최종 −93.8% 가 거기 기대고 있지는 않다.',
      '⚠️ 이 축은 프레임당 Average 라 초반 저부하 프레임이 표본의 다수를 차지한다. 후반의 레이어 격차는 07 의 구조 표로 봐야 한다.',
    ],
  },

  /* ── S4 네 방식 한눈에 (Level 2) — 지도 ──────────────────────────────── */
  s4: {
    gist: '네 방식은 서로 다른 프레임의, 서로 다른 비용을 건드린다.',
    lead: '막대 네 칸이 각각 무엇이었는지를 한 장에 놓는다. 가로축은 **프레임 두 종류**다 — 접는 선을 끄는 동안(보간)과 놓는 한 프레임(확정).',
    map: {
      // 지도에 수치를 넣지 않는다 — 크기는 02 가 이미 줬고, 여기는 위치만 준다(§03 S4 REMOVE).
      rows: [
        { n: '①', side: 'lerp', title: '안 바뀐 레이어는 다시 만들지 않는다', what: '매 프레임 전 레이어를 다시 만들던 것' },
        { n: '②', side: 'commit', title: '가려진 레이어를 확정 때 지운다', what: '회차마다 쌓이던 레이어 장수' },
        { n: '③', side: 'lerp', title: '레이어당 렌더 오브젝트를 버린다', what: '레이어 한 장마다 붙던 유니티 객체' },
        { n: '④', side: 'lerp', title: '분할을 Burst 잡과 네이티브 버퍼로', what: '매 프레임 새로 만들던 관리형 객체' },
      ],
      lerpLabel: '보간 프레임 — 선을 끄는 동안 여러 장',
      commitLabel: '확정 — 놓는 한 장',
    },
    vizCaption: '①③④ 는 **선을 끄는 동안 매 프레임 도는 비용**이고, ② 만 **확정하는 한 프레임**의 비용이다. 이 축이 10 의 판단까지 그대로 이어진다 — 확정 때 한 번 비싼 것과 매 프레임 조금 비싼 것 중 무엇이 나은가는 **접는 빈도**가 정한다.',
    after: '뒤에 둘이 더 붙는다. **09** 는 이 넷을 다 재고 난 뒤 재는 도구 자체를 의심해 전부 다시 잰 것이고, **10** 은 ②가 만든 판정 비용을 어떤 방식으로 낼지 다시 고른 것이다. 둘 다 단계 막대에는 들어가지 않는다.',
  },

  /* ── S5 진단 (Level 2) — §02 M2′ ─────────────────────────────────────── */
  s5: {
    gist: '예상은 폴리곤 분할이었고, 실측은 렌더링 준비였다.',
    body: '왜 그 넷을 그 순서로 골랐는지가 여기서 시작된다. 회차를 거듭할수록 레이어가 쌓이니 자르는 계산이 무거워졌으리라 보고 폴리곤 분할부터 의심했는데, 마커를 나눠 재 보니 순서가 반대였다.',
    vizCaption: '세 마커를 같은 축에 놓으면 예상한 자리와 실제로 비싼 자리가 어긋난다. 비싼 곳은 **자르는 계산이 아니라 그 결과를 화면에 올릴 준비**였다.',
    table: {
      headers: ['프레임당 마커', '무엇을 재는가', '회차 16 · Average'],
      rows: [
        ['`Renderer.Sync`', '레이어를 메시로 만들어 올리는 준비', '**0.4525 ms**'],
        ['`FoldOperation.Split`', '접는 선으로 폴리곤을 자르는 계산', '0.1700 ms'],
      ],
    },
    caption: '예상은 분할이었다. 실측에서는 렌더링 준비가 **2.7배** 비쌌다.',
    revised: '진단 당시에는 이 차이가 **8배**로 보였다. 그때 쓰던 벤치는 뒤에 퇴화 패턴이라 폐기했고, 마커 안에는 아직 `Debug.Log` 한 줄이 들어 있었고, 통계도 Median 이었다. 셋을 다 걷고 같은 진단을 다시 내니 2.7배다 — **정도는 3분의 1로 줄었지만 방향은 바뀌지 않았다.** 그 걷어내는 과정이 09 다.',
    after: '정체는 기하 계산량이 아니었다. 같은 회차의 정점 총수는 1,348개인데 레이어는 337장이었고, 비용은 레이어 **한 장마다 붙는 유니티 객체** 쪽에 있었다. 그래서 GPU 문제로 넘기지 않고 CPU 쪽으로 방향을 잡았다 — GPU 는 가려진 픽셀을 z-buffer 로 버리지만, CPU 는 화면에 보이지도 않을 레이어의 메시까지 매번 새로 만들고 있었다.',
    kernel: '접기 자체 — 접는 선으로 폴리곤을 자르고 교점을 정점과 UV 에 같은 매개변수로 먹여 접힌 자리에서 텍스처가 어긋나지 않게 하는 커널 — 은 원작에서 내가 다시 쓴 것을 그대로 옮겨 왔다. 아래 넷은 전부 그 위에서 한 것이다.',
  },

  /* ── S6 ① 재사용 (Level 3) · 385fd68 · 2026-07-22 ────────────────────── */
  s6: {
    link: '진단이 지목한 것은 레이어 한 장마다 붙는 객체였다. 가장 먼저 한 것은 그 객체를 없애는 게 아니라 **손대지 않아도 되는 것을 골라내는 일**이었다.',
    vizCaption: '접는 선에 실제로 걸치는 레이어는 일부인데, **바뀐 것을 알아볼 방법이 없어 매 프레임 전부 다시 만들고 있었다.** 걸치지 않는 레이어를 원본 그대로 통과시키면 뷰가 참조만 비교해 건너뛸 수 있다.',
    pair: {
      file: 'FoldOperation.cs — 레이어 루프',
      commit: '385fd68 · 2026-07-22',
      intro: '뷰가 "지난 프레임과 같은가" 를 물으려면 근거가 있어야 하는데, 연산이 매번 새 객체를 뱉으니 물어볼 수가 없었다. 그래서 뷰가 아니라 연산의 반환 규약을 먼저 고쳤다. (줄바꿈은 지면에서 다시 끊었다)',
      before: {
        ref: '레이어마다 자르고, 결과는 매번 새 인스턴스',
        code: `PaperGeometry.SplitPolygonByLine(
    layer.Vertices, layer.UVs,
    midPoint, abDirection, foldAxis,
    out polyA,     out uvsA,
    out flippedB,  out flippedUvsB);

if (polyA.Count > 2)
    fixedLayers.Add(
        new PaperLayer(polyA, uvsA,
                       layer.IsFolded, i));`,
      },
      after: {
        ref: '자르기 전에 어느 쪽인지 먼저 본다',
        code: `PaperGeometry.PolygonSide side =
    PaperGeometry.ClassifyPolygon(
        layer.Vertices, midPoint, abDirection);

// 통째로 남는 쪽 — 원본 인스턴스를 그대로 넘긴다.
// 참조가 보존돼야 뷰가 "지난번과 같은 레이어"임을
// 알아보고 메시 재생성을 건너뛴다.
if (side == PaperGeometry.PolygonSide.Front)
{
    fixedLayers.Add(layer);
    continue;
}`,
      },
      result: '뷰 쪽은 if (ReferenceEquals(_lastLayer, layer)) return; 한 줄로 끝난다',
    },
    notes: [
      '고친 자리는 뷰가 아니라 **연산의 반환 규약**이다. 뷰가 물어볼 근거를 연산이 먼저 만들어 줬다.',
      '`PaperLayer` 는 불변이라 참조가 같으면 내용도 같다 — 그래서 참조 비교가 정당한 근거가 된다. 같은 커밋이 `StackOrder` 필드도 지웠다. 값이 리스트 인덱스와 항상 같아 파생할 수 있었고, 그것이 레이어당 객체 할당을 하나 더 없앴다.',
      '⚠️ 이 변경은 **절반이 죽었다.** 뷰 쪽 건너뛰기는 07(병합)이 무효로 만들었고 — 합쳐 그리면 매 프레임 전부 다시 쌓는다 — **연산 쪽 반환 규약은 지금도 돌아간다.** 02 의 막대에서 44.7% 가 이 단계의 몫인 것은 그 죽은 절반을 포함한 값이다.',
    ],
  },

  /* ── S7 ② 삭제 (Level 3) · 992c50a · 2026-07-23 ──────────────────────── */
  s7: {
    link: '①까지는 전부 **"장수 × 상수"의 상수 쪽**이었다. 장수는 접을수록 1.5배씩 늘어 언젠가 상수 개선을 삼킨다. 여기서 처음 장수 쪽을 건드린다.',
    vizCaption: '위아래 양쪽 모두에 가려진 레이어는 **어떻게 접어도 다시 드러나지 않는다** — 넘어간 조각의 위아래가 뒤집혀도 서로 자리를 바꿀 뿐이다. 그래서 확정하는 순간 데이터에서 지워도 화면이 그대로다. 회차 16 기준 337장이 57장으로 줄었다.',
    pair: {
      file: 'PaperController.ConfirmFold + PaperData.PruneBuried',
      commit: '992c50a · 2026-07-23',
      intro: '위 레이어부터 차례로 빼 나가면 "보이는 면적"과 "완전히 가려진 레이어"가 같은 계산에서 나온다. 따로 만들면 같은 계산을 두 번 짓는 꼴이라 하나로 합쳤다.',
      before: {
        ref: '확정하면 그대로 쌓인다',
        code: `_confirmed = FoldOperation.Preview(
    _baseData, localA, localB, _allowFullFlip);
_baseData = _confirmed;`,
      },
      after: {
        ref: '확정할 때 파묻힌 것을 걷어낸다',
        code: `_confirmed = FoldOperation
    .Preview(_baseData, localA, localB,
             _allowFullFlip)
    .PruneBuried();
_baseData = _confirmed;

// ── PruneBuried 안 ──
// 버릴 것이 없으면 원본 그대로.
// 레이어 참조가 보존돼야 뷰가 메시를 다시 안 만든다
if (keptCount == _layers.Count) return this;`,
      },
      result: '벤치 16회차 5,041장이던 시퀀스가 3회차 3장에서 수렴했다 — 증폭이 사라졌다',
    },
    notes: [
      '살아남은 레이어의 가시성은 **다시 계산하지 않고 물려준다.** 파묻힌 것을 지워도 나머지가 가려진 정도는 변하지 않기 때문이고, 다시 계산하면 확정 한 번에 전 레이어 폴리곤 연산이 두 번 돈다.',
      '⚠️ 대가가 있다. 지워진 레이어가 살아 있는 유일한 곳이 되돌리기 히스토리라, 같은 커밋이 히스토리를 `Stack` 에서 `List` 로 바꾸고 한도 10을 뒀다 — **한도를 넘겨 밀려난 상태는 영영 되돌릴 수 없다.**',
      '이 시점의 가려짐 판정은 범용 폴리곤 불리언(Clipper2) 이었다. 그 방식을 바꾸는 것은 한참 뒤인 **10** 이다.',
    ],
  },

  /* ── S8 ③ 병합 (Level 3) · 792bb3a · 2026-07-24 ──────────────────────── */
  s8: {
    // ⚠️ L3 절에는 요지 줄(.cm-gist)을 두지 않는다(§03 시각계층표) — 방식 6절의 리듬이 깨진다.
    //    이 절의 KEY MESSAGE("레이어가 몇 장이든 렌더 오브젝트는 2개")는 vizCaption 과 표가 진다.
    link: '②가 지운 뒤에도 렌더 오브젝트는 77개가 남아 있었다. 04 가 지목한 정체 — 레이어 한 장마다 붙는 유니티 객체 — 를 여기서 정면으로 없앤다.',
    vizCaption: '레이어 한 장마다 GameObject·MeshFilter·Mesh 를 두던 것을 **앞면·뒷면 메시 두 개**로 합쳤다. 쌓임 순서는 정점 z 에 굽고, 무엇이 무엇을 가리는지는 z-buffer 가 정한다 — 그래서 **레이어가 몇 장이든 렌더 오브젝트는 2개**다.',
    pair: {
      file: 'PaperRenderer.cs — 뷰 풀 → 앞뒤 2메시',
      commit: '792bb3a · 2026-07-24',
      intro: '무거운 것은 무엇을 그리느냐가 아니라 몇 개의 객체로 그리느냐였다. 그러면 객체 수를 데이터 크기에서 떼어내면 된다.',
      before: {
        ref: '보이는 레이어 수만큼 GameObject 를 만든다',
        code: `EnsureCapacity(CountVisible(data, canCull));
// …
while (_views.Count < requiredCount)
{
    GameObject go = new GameObject(
        $"Layer{_views.Count}");
    PaperLayerView view =
        go.AddComponent<PaperLayerView>();
    view.Initialize();   // + MeshFilter/Renderer/Mesh
    _views.Add(view);
}`,
      },
      after: {
        ref: '메시는 둘뿐이고, 쌓임은 정점 z 에 굽는다',
        code: `_frontMesh = CreateMeshObject("PaperFront", …);
_backMesh  = CreateMeshObject("PaperBack",  …);
// …
for (int i = 0; i < data.LayerCount; i++)
{
    // 쌓임 순서 = 레이어 인덱스.
    // z 로 구워 z-buffer 가 위 레이어를 남긴다
    float z = -i * _stackSpacing;
    if (layer.IsFolded)
        AppendLayer(layer, z, _backPositions,  …);
    else
        AppendLayer(layer, z, _frontPositions, …);
}`,
      },
      result: '같은 커밋이 PaperLayerView.cs 229줄을 통째로 지웠다',
    },
    notes: [
      '앞뒤를 구분하는 수단은 **두 메시의 머티리얼 하나뿐**이다. 넘어간 조각도 winding 이 앞면으로 맞춰져 있어 컬링으로는 가를 수 없다.',
      '가림 판정을 CPU 가 하지 않고 **z-buffer 에 떠넘겼다.** "보이는 면만 그리기"(폴리곤 불리언)도 후보였지만, 정점이 251개뿐이라 그 계산이 아끼는 것보다 비싸리라 보고 **재지 않았다** — 뒤에 10 에서 같은 계열의 판정 비용을 실제로 재 보니 그 판단과 같은 방향이었다.',
      '⚠️ ①의 뷰 쪽 최적화가 여기서 무효가 됐다. 합쳐 그리면 매 프레임 전 레이어를 다시 쌓기 때문이다. 정점이 세 자리라 실질 손실이 없다고 보고 받아들였다.',
    ],
    table: {
      headers: ['지표 · 회차 16', '이전', '지운 뒤', '병합한 뒤'],
      rows: [
        ['렌더 오브젝트 수', '337', '77', '**2**'],
        ['드로우콜 델타 (정지 대조군 대비)', '+298', '+71', '**+1**'],
        ['레이어 수', '337', '57', '57'],
        ['정점 수', '1,348', '251', '251'],
      ],
    },
    reconcile: '위 두 행에서 **지우기(②)가 먼저 4분의 3을 하고**, 병합이 나머지를 장수와 무관하게 만든다. 아래 두 행은 지우기만의 몫이다 — 병합은 그리는 방식만 바꾸므로 데이터가 달라지면 안 되고, 실제로 안 달라졌다. **렌더 오브젝트 77 과 레이어 57 이 다른 이유**는 뷰 풀이 끄는 동안의 최대치에 맞춰 커진 뒤 줄지 않기 때문이다.',
    policyNote: '이 표의 레이어·정점은 **당시 런타임의 가려짐 판정 방식**(10 에서 ①로 부르는 것) 기준이다. 10 에서 채택한 볼록 뺄셈으로는 41장이 남는다 — 위 57 과 다른 것은 판정 방식이 달라서지 모순이 아니다. ①의 열이 없는 것은 그 변경이 무엇을 다시 만드느냐만 바꿔 이 지표들이 안 움직이기 때문이고, ①의 몫은 02 의 막대에서만 보인다.',
  },

  /* ── S9 ④ Burst (Level 3) · cafa4ae · 2026-07-24 ─────────────────────── */
  s9: {
    link: '렌더 쪽을 끝내고 나면 보간 프레임에 남는 것은 분할 계산 하나다. **선이 움직이는 매 프레임** 도는 유일한 연산이라 여기만 잡으로 옮겼다.',
    intro: '보간 프레임마다 `PaperData` · `PaperLayer` · `List<Vector2>` 를 레이어 수만큼 새로 만들고 있었다 — 프레임당 25~50KB, 400~900회 할당. 분할을 `NativeArray` 위 Burst 잡으로 옮기고 **결과를 관리형으로 되돌리지 않기로** 했다. 확정하는 순간에만 `PaperData` 로 마샬한다. 렌더러는 항상 네이티브 뷰에서 그리므로 그리는 코드는 하나다.',
    vizCaption: '레이어마다 쓸 칸이 미리 정해져 있어 스레드끼리 구간이 겹치지 않는다. 락이 없고, **결과 순서가 스레드 속도와 무관하게 결정된다.** 쌓임 순서가 곧 종이의 앞뒤라 이 결정성은 선택이 아니다.',
    slot: {
      title: 'PaperSplitLayersJob.cs — 출력 자리 사전 배정',
      intro: '레이어 i 는 자기 몫의 두 칸에만 쓴다. 스레드끼리 구간이 안 겹쳐 경쟁이 없고, 결과가 놓이는 자리가 스레드 속도와 무관하게 정해진다.',
      code: `public void Execute(int index)
{
    int2 range       = BaseRanges[index];
    int capacity     = range.y + SlotMargin;
    int fixedStart   = SlotStarts[index];
    int flippedStart = fixedStart + capacity;

    PaperGeometryNative.SplitConvex(/* … */
        fixedStart, flippedStart, capacity,
        out int fixedCount, out int flippedCount);
}`,
      result: '락 없이 결정론 — 같은 입력이면 스레드가 어떤 순서로 끝나든 같은 결과다',
    },
    pair: {
      file: 'PaperController.cs — 매 프레임 경로',
      commit: 'cafa4ae · 2026-07-24',
      intro: '기준 상태는 접기를 시작해서 확정할 때까지 고정이다. 그래서 폴드당 한 번만 올리고, 매 프레임 바꾸는 것은 접는 선 세 값뿐이다. 스케줄과 회수를 프레임 안에서 떼어 놓아 그 사이에 메인 스레드가 다른 일을 한다.',
      before: {
        ref: '매 프레임 관리형 상태를 새로 만든다',
        code: `UpdateFoldLineCache(
    _animator.PointA, _animator.CurrentPointB);

SetPresented(FoldOperation.Preview(
    _baseData,
    _animator.PointA, _animator.CurrentPointB,
    _allowFullFlip));`,
      },
      after: {
        ref: '스케줄(NormalTick)과 회수(LateTick)를 떼어 놓는다',
        code: `// NormalTick — 결과를 기다리지 않는다.
// 워커가 도는 동안 메인은 남은 일을 한다
UpdateFoldLineCache(
    _animator.PointA, _animator.CurrentPointB);
ScheduleSplit(
    _animator.PointA, _animator.CurrentPointB);

// LateTick — 스케줄에서 여기까지의 거리가
// 곧 워커에게 준 시간이다
_pipeline.Complete(_allowFullFlip);
_paperRenderer.Sync(_pipeline.View);`,
      },
      result: '버퍼는 Allocator.Persistent — TempJob 은 4프레임 제한이라 수백 프레임짜리 드래그에 못 쓴다',
    },
    notes: [
      '보간 중 잠정 상태는 **관리형으로 만들어지지 않는다.** 네이티브 버퍼에만 있고 확정 순간에만 옮긴다. 쌓임 순서도 자료구조가 아니라 **순회 방향**으로 만들었다 — 제자리 조각은 아래에서 위로, 넘어간 조각은 위에서 아래로 훑으면 리스트를 뒤집거나 복사하는 일이 한 번도 없다.',
      '확정도 **보간과 같은 잡**을 쓴다. 분할 구현이 하나뿐이라야 눈에 보이던 모양과 저장되는 모양이 어긋날 수 없다.',
      '⚠️ Job 을 쓰려면 "언제 스케줄하고 언제 걷는가"를 프레임 안에서 선언할 수단이 필요했는데 유니티 기본 `Update` 에는 그것이 없다. 그래서 자체 생명주기 훅을 두고 그 위에 얹었다 — **그 훅 자체는 재지 않았고 이 페이지의 수치에 들어 있지 않다.**',
    ],
    results: [
      '`FoldOperation.Split` **0.0403 → 0.0176 ms**',
      '보간 프레임 할당(중앙값) — 원값 **39,006B / 613회 → 25,468B / 403회**. 에디터가 정지 상태에서도 내는 바닥 17,772B / 372회를 양쪽에서 빼면 **21,234B / 241회 → 7,696B / 31회**다. ⚠️ 뺀 바닥이 개선 후 원값의 70% 라 이 차분은 바닥이 흔들리면 같이 흔들린다. 바닥이 상수라는 근거는 정지 프레임 열세 개가 바이트까지 같았다는 것뿐이다.',
      '`Compose` 는 0.0011 → 0.0016 ms 로 늘었다. 참조를 옮기던 일이 조각을 훑는 일로 바뀐 몫이고, 절대값이 작아 합에는 영향이 없다.',
      '메시 단계는 "가려낸 뒤 병렬로 처리"를 골랐다. 기각한 쪽("전부 재계산")은 설령 더 빠르더라도 메모리를 더 쓴다고 봤는데, **양쪽 다 재지 않았다.** 배치 크기 16 도 재 보고 고른 값이 아니다.',
    ],
  },

  /* ── S10 ⑤ 재측정 (Level 3) · f920035 · 2026-07-24 ───────────────────── */
  s10: {
    link: '넷을 다 고치고 다 잰 뒤였다. 05 에서는 원인을, 06~08 에서는 방법을 의심했다. 마지막으로 남은 것이 **재는 도구 자체**였다.',
    split: '`Renderer.Sync` 가 왜 비싼지 모르는 채로 범인을 짚지 않았다. 먼저 `Sync` 안을 `Fill` · `Upload` · `Log` 세 마커로 쪼갰다. 재 보니 `Fill` 과 `Upload` 를 합쳐도 `Sync` 의 10% 였다. 남은 90% 는 프레임마다 한 줄 도는 `Debug.Log` 였다 — 마지막 단계의 보간 프레임 중앙값 229,300ns 중 205,900ns, **89.8%.** 레이어가 많은 초기 단계에서는 비중이 이보다 낮다(S0 에서 약 28%).',
    pair: {
      file: 'PaperRenderer.cs — 로그 게이트',
      commit: 'f920035 · 2026-07-24',
      intro: '게이트가 로그 함수 안에 있으면 이미 늦다. C# 은 인자를 먼저 다 계산하므로, 로그가 꺼져 있어도 보간 문자열은 만들어진다. (로그 문자열은 원본을 줄인 것이다)',
      before: {
        ref: '호출부는 꺼진 줄 알지만 문자열은 이미 만들어졌다',
        code: `// 호출부 — 매 프레임 도는 자리
this.LogIf(_isDebugLogging,
    $"Sync 완료 - 조각:{view.PieceCount}, …", this);

// 그 LogIf 의 본문
public static void LogIf(
    this object self, bool cond, string msg, …)
{
    if (!cond) return;   // ← 여기 닿았을 때는
                         //   msg 가 이미 완성돼 있다
    Debug.Log(Format(self, msg, LogLevel.Info), …);
}`,
      },
      after: {
        ref: '게이트를 호출부 밖으로 끌어냈다',
        code: `if (CLog.IsEnabled(GameLogChannel.Paper,
                   LogLevel.Verbose))
{
    CLog.Verbose(GameLogChannel.Paper,
        $"Sync 완료 - 조각:{view.PieceCount}, …", this);
}

// 범인을 짚기 전에 먼저 한 것 — 계측을 쪼갰다
static readonly ProfilerMarker FillMarker   = …;
static readonly ProfilerMarker UploadMarker = …;
static readonly ProfilerMarker LogMarker    = …;`,
      },
      result: '이 한 줄이 마지막 단계에서 Renderer.Sync 비용의 89.8% 였다',
    },
    notes: [
      '순서가 핵심이다. **범인을 짚기 전에 계측을 먼저 쪼갰다** — 세 마커로 나눠 재고 남은 90% 가 어디인지 그림이 가리키게 한 뒤에 그 한 줄을 찾았다.',
      '그 한 줄은 레이어 수와 무관하게 **모든 단계에 프레임당 한 번씩 똑같이** 들어 있었다. 상수가 양쪽에 더해진 꼴이라 개선률이 실제보다 작게 나오고 있었다.',
      '고친 뒤 **얼려 둔 브랜치 넷을 그대로 다시 돌렸다.** 엔진은 옛 커밋 그대로, 로깅만 최신으로 맞춰서다 — 01 의 얼리기 장치가 여기서 값을 한다. 05 의 진단도 이때 다시 냈다.',
    ],
    delta: {
      label: 'S0 대비 총 개선 — 같은 코드, 다시 잰 값',
      before: { k: '재측정 전', v: '−70.0%' },
      after: { k: '재측정 후', v: '−93.8%' },
      note: '두 값은 **같은 코드를 두 번 잰 것**이지 추가 개선이 아니다. 재측정은 내 성과를 **키웠다** — 규율이 자기에게 유리한 쪽으로만 작동한 게 아니라는 증거가 같은 사건 안에 있다.',
    },
    rules: [
      ['그 대조군이 서 있는 벤치부터 의심한다', '처음 쓰던 정렬 접기 벤치는 조각이 정확히 포개지는 퇴화 패턴이라, 가려진 레이어를 지우기 시작하자 3회차에서 수렴해 끝나 버렸다. 도달할 수 없는 상한을 성과로 쓰는 꼴이라 벤치를 버리고 임의 접기로 새로 만들었다. 05 의 "8배"가 그 폐기된 벤치의 값이다.'],
      ['결론은 Average 로만 낸다', '`Split` 을 노이즈 대조군으로 두고 봤더니 Average 는 ±3% 안이고 Median 과 Max 는 30% 안팎 흔들렸다. 에디터에서 Max 는 쓸 수 없는 지표다. 단 **스파이크와 구간 대표값은 중앙값으로 본다** — 10 의 정책 비교가 그 자리다. ⚠️ 대조군 자격은 `Split` 의 코드가 안 바뀐 구간에서만 성립한다 — 08 에서 그 코드를 갈아치웠다.'],
      ['내 추론도 같은 규율에 건다', '`Sync` 가 −27% 인 것을 "정점 그릇이 바뀐 덕" 이라고 적었다가, 재측정에서 −3.3% 로 나왔다. 대조군이 흔들리는 폭과 같은 크기라 기각하고 문서에 정정을 남겼다.'],
    ],
    cost: '재측정에도 대가가 있었다. 새 시리즈는 마커를 셋만 재므로 `LayerView.RegenerateMesh` 열이 사라졌고, 그래서 초기 단계의 `Sync` 내역을 더 잘게 갈라 볼 수는 없게 됐다.',
  },

  /* ── S11 ⑥ 판정 교체 (Level 3) · 0d73c39 · 2026-08-12 ────────────────── */
  s11: {
    link: '재측정으로 규율이 선 뒤에 남은 숙제가 하나 있었다. 06 이 만든 **확정 시 판정 비용**을 어떤 방식으로 낼 것인가다. 방식이 셋 있었고 고르기 전에 셋을 다 쟀다.',
    // KEY MESSAGE("레이어가 전부 볼록이라 범용 불리언은 애초에 필요 없었다")는 notes[0] 이 진다 — L3 라 요지 줄 없음.
    vizCaption: '덮개가 볼록이면 **변마다 한 번씩 자르는 것**으로 나머지가 정확히 구해진다. 이번 변 바깥은 조각으로 떼어 내고 안쪽만 다음 변으로 넘기면 되므로, **교차점을 따로 구할 필요도 도형을 다시 조립할 필요도 없다.** 남은 것이 없으면 그 레이어는 완전히 덮인 것이다.',
    pair: {
      file: 'PaperVisibility.cs → PaperCoverage.cs',
      commit: '0d73c39 · 2026-08-12',
      intro: '①은 범용 폴리곤 불리언으로 덮개를 합집합으로 쌓고 차집합을 뺀다. ②는 덮개의 변을 차례로 반평면 경계로 삼아 자르기만 반복한다.',
      before: {
        ref: '① 누적 합집합 + 차집합 (Clipper2)',
        code: `// 한 장씩 합쳐 하나의 영역으로 눌러둔다
covered = Clipper.Union(
    covered, new Paths64 { path }, FillRule.NonZero);

// 덮인 영역을 뺀 나머지 넓이
Paths64 remain = Clipper.Difference(
    new Paths64 { path }, covered, FillRule.NonZero);
if (remain.Count == 0) return 0f;`,
      },
      after: {
        ref: '② 볼록 뺄셈 — 변마다 반평면으로 한 번씩',
        code: `for (int e = 0; e < coverCount; e++)
{
    EdgeNormal(coverVertices, coverStart,
               coverCount, e, coverSign,
               out float2 a, out float2 normal);

    // 이번 변 바깥 = 떨어져 나가는 조각
    int outsideCount = ClipHalfPlane(/* … */
        a, normal, keepInside: false);

    // 안쪽만 남겨 다음 변으로 넘긴다
    int insideCount = ClipHalfPlane(/* … */
        a, normal, keepInside: true);

    // 안쪽이 비면 남은 항도 전부 비어 있다
    if (insideCount < 3) return true;
}`,
      },
      result: '같은 커밋이 Cartapli.Paper.asmdef 에서 Clipper2 참조를 뺐다 — 이제 실수로 쓰면 컴파일이 안 된다',
    },
    notes: [
      '①을 기각한 진짜 근거는 속도가 아니라 **불변식**이다. 초기 종이가 볼록이고 분할이 볼록을 볼록 둘로만 가르므로, 접기를 아무리 반복해도 **레이어가 볼록에서 벗어나지 않는다.** 그 위에서는 반평면 자르기만 반복하면 되고, 근사가 아니라 정확하다.',
      '**선행 겹침 판정이 필수다.** 건너뛰면 안 겹치는 덮개가 조각을 변 수만큼 산산조각 낸다 — 뺄셈 분해가 변마다 한 조각씩 떼어내기 때문이고, 경계상자만으로는 이 경우를 못 거른다.',
      '조각 수는 원리적으로 폭발할 수 있어(덮개가 조각 한가운데에 들어가면) 작업 버퍼를 고정 크기로 두고 **넘치면 "안 덮임"으로 물러선다.** 판정이 곧 삭제 허가라 **두 오류의 값이 비대칭**이기 때문이다 — 덮인 걸 놓치면 레이어 한 장이 남을 뿐이지만, 안 덮인 걸 덮였다고 하면 종이가 화면에서 사라진다.',
    ],
    table: {
      headers: ['정책', '판정 비용', '지운 레이어 (16회 누적)', '남는 레이어 (회차 16 시점)', '판정'],
      rows: [
        ['① 누적 합집합·차집합 (Clipper2)', '②의 12~23배', '69장', '57장', '기각 — 느린데 지운 장수도 더 적다'],
        ['② 볼록 뺄셈 — 여러 장이 합쳐 덮는 경우까지', '기준', '98장', '41장', '**채택**'],
        ['③ 단일 덮개 — 한 장이 통째로 품는 경우만', '②의 0.37~0.45배', '기록 없음', '57장', '기각 — 판정은 싸지만 뒤가 따라 오른다'],
      ],
    },
    tablecap: '두 수치 열은 **더할 수 없다** — 왼쪽은 16회 누적, 오른쪽은 회차 16 시점의 잔량이다. ③의 누적 지운 장수는 남기지 않았다.',
    decide: '**승패를 가른 것은 판정 자체의 속도가 아니라 접기 빈도였다.** ③은 판정이 ②보다 싸지만 덜 지운다. 남는 레이어가 41장에서 57장으로 늘면 그만큼 `Split` 과 `Renderer.Sync` 가 따라 오른다. ③의 매 프레임 손해가 ②의 확정 1회 손해를 갚는 데 19프레임(60fps 에 0.32초)이 걸리는데, 실제 설계는 20~30초에 한 번 접으므로 손익분기보다 60~100배 뜸하다. 분당 3회 기준 회차 16 의 프레임 평균은 ② 38.5µs, ③ 45.9µs 다(확정 프레임 비용을 분당 3회로 상각해 매 프레임에 나눠 붙인 값이다). ⚠️ 이 20~30초는 **설계값이지 실플레이 계측이 아니다** — 이 선택 전체가 그 값 위에 서 있다.',
    tradeoff: '확정 1프레임의 **판정 최대**로 보면 ③이 낫다 — ② 0.247 ms 대 ③ 0.083 ms. 판정은 확정하는 한 프레임에 몰리기 때문이다. 표의 "②의 0.37~0.45배" 와 비율이 다른 것은 재는 대상이 아니라 **하네스가 달라서다** — 표는 세 방식을 따로 돌려 잰 값이고 이쪽은 실벤치 진단 런이다. ②는 그 스파이크를 안고 상시 비용을 아끼는 쪽이다.',
    timing: '⚠️ 이 비교는 **2026-08-12 측정이고 세 방식을 따로 돌린 별도 하네스**다. 02 의 막대와 07 의 표는 그보다 앞선 2026-07-24 측정이라 **①이 런타임이던 상태**의 값이다(같은 `Split` 을 ②에서 다시 재면 0.0121 ms 다). 그래서 이 교체는 단계 막대에 얹지 않았다 — 얹으면 다섯 막대의 측정 조건이 거짓이 된다.',
    revert: {
      title: '되돌리는 조건',
      body: '접기 상한이 5회 이하로 확정되거나 접기 간격이 0.3초 아래로 잦아지면 ③으로 되돌린다. ③의 코드(`PaperCoverage.IsCoveredBySingle`)를 지우지 않고 남겨 뒀고, 정책 비교 테스트가 셋을 다 돈다.',
    },
  },

  /* ── S12 검증과 한계 (Level 4) ───────────────────────────────────────── */
  s12: {
    lead: '**여섯 번 갈아치웠으면** 다음 질문은 그 숫자들이 같은 그림에 대한 것인가다. 08 에서 분할 구현을, 10 에서 판정 방식을 통째로 바꿨다.',
    body: '그러면 결과가 조용히 달라질 수 있다. 쌓임 순서가 틀리면 화면에서는 색이 살짝 다른 정도로만 보여 눈으로 못 잡는다. 그래서 두 경우 모두 **관리형 구현을 지우지 않고 테스트 오라클로** 남겼다 — `FoldOperation` 과 `PaperVisibility` 가 그것이다. 후자는 런타임 어셈블리에서 아예 빠져 테스트 쪽으로 옮겨 갔다. 대가는 런타임에서 빠진 코드를 계속 유지·컴파일하는 것이다.',
    tests: [
      ['`PaperGeometryNativeTests`', '폴리곤 한 장 분할을 관리형 `PaperGeometry` 와 대조', '임의 400 케이스'],
      ['`PaperFoldSplitPipelineTests`', '파이프라인 전체를 `FoldOperation.Preview` 와 대조', '임의 200 케이스'],
      ['`PaperVisibilityTests`', '가려짐 판정을 관리형 기준 구현과 대조', '14 케이스'],
    ],
    why: '파이프라인 테스트가 따로 있는 이유는 폴리곤 한 장짜리 오라클로는 **쌓임 순서를 검증할 수 없기** 때문이다. 단계마다 회차별 레이어·정점 수도 대조한다 — 결정론 시퀀스라 기하 의미가 조금이라도 달라지면 숫자가 갈린다.',
    limitsLead: '검증이 닿은 범위 다음이 안 닿은 범위다.',
    limits: [
      ['측정 환경', '전부 Windows PC · Unity 에디터 PlayMode 다. 기기 실측과 GPU 시간은 없다. 전후 비교가 성립하는 근거는 하드웨어가 아니라 모든 단계를 같은 절차로 다시 쟀다는 것이다.'],
      ['표본 수', '각 단계 **단일 실행**이다. 05 의 진단도 한 번 돌린 결과다. 노이즈 폭은 대조군(`Split`)으로만 추정했다.'],
      ['확정 프레임 할당', 'Burst 잡의 범위 밖이라 안 줄었다 — "접기의 GC 쓰레기를 없앴다" 고 말하면 거짓이 된다. ⚠️ 자주 인용되는 1.73MB / 23,978회 는 **확정 프레임 최대**이고, 10 에서 기각한 ① 이 런타임이던 시절 값이다. ② 로 바꾼 뒤 다시 잰 값은 **후반 평균 ~28,200 B** 인데, **최대와 후반 평균은 통계가 달라 두 수를 이어 붙일 수 없다.** **①에서 ②로 바뀐 전후를 같은 통계로 잰 기록이 없다**는 것이 여기서의 한계다.'],
      ['드로우콜', '에디터에서 정지 대조군과의 **델타**로만 쟀다. development build 나 기기 실측은 없다.'],
      ['접기 빈도', '분당 2~3회는 **설계값이지 실플레이 계측이 아니다.** 10 의 정책 선택이 이 값에 걸려 있다.'],
      ['판정 비용 곡선', '회차 5 · 10 · 16 세 점만 실측하고 나머지는 보간했다. 게다가 그 표의 구간이 회차와 정확히 대응한다는 보장이 없다 — 샘플 인덱스를 진행도의 대리값으로 썼다.'],
      ['오버드로우', '재지 않았다. 10 에서 기각한 단일 덮개 정책에 유리한 방향의 미확인이다.'],
      ['잔여 할당', '보간 프레임에 남은 7,696B / 31회의 정체는 규명하지 않았다. 작고 상수라 파지 않았다.'],
      ['생명주기 훅', '08 이 얹혀 있는 자체 프레임 훅은 **성능을 재지 않았다.** 이 종이접기 벤치로는 잴 수 없다 — 매 프레임 도는 객체가 컨트롤러와 뷰뿐이다.'],
    ],
  },
};
