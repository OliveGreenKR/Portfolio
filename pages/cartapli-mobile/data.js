// pages/cartapli-mobile/data.js
// ─────────────────────────────────────────────────────────────────────────────
// 이 파일이 이 페이지 문장의 유일한 원본이다. 중간 문서를 만들지 않는다.
// 상류 판단(사실 → 전략 → 구조)의 원본:
//   knowledge_base/projects/cartapli_mobile/research/portfolio-flow.md
//   §01 사실 · §02 전략 · §03 섹션 계약
//
// ── 수치 규칙 ────────────────────────────────────────────────────────────────
// 1. 시간·할당 수치가 나오는 자리마다 아래 한 문장을 붙인다. 문구를 바꾸지 않는다.
//      "Windows PC · Unity 에디터 PlayMode · 임의 접기 벤치(시드 12345) 16회차"
//    → 상수 COND. 조건을 떼면 E3(동일 조건 전후 비교) 자격을 잃는다.
// 2. 구조 불변식(s4.table — 렌더 오브젝트·드로우콜·레이어·정점)에는 COND 를 붙이지 않는다.
//    플랫폼 무관이라 조건이 필요 없고, 붙이면 오히려 약해 보인다.
// 3. §01 에 없는 수치를 쓰지 않는다. 없으면 없다고 적는다(s8.note 가 그 예).
//
// ── 문장 규칙 ────────────────────────────────────────────────────────────────
// 4. 주장 세기 ≤ 증거 급. E1(구조적 불변식)에서는 방향·불변식만 쓰고 "정도"는 못 쓴다.
//    금지 어휘: 크게 개선 · 성능 향상 · 최적화했다 · 효율적으로 · 획기적으로 ·
//               강력한 · 최적의 · 다양한 · 사용자 경험을 향상 · 많은 것을 배웠다
// 5. renderInline 은 **굵게** · `코드` · *기울임* 만. 중첩 금지, 목록기호·링크·취소선 미지원.
//    AsciiBlock 의 intro / result 는 renderInline 을 안 거친다 — 마크업을 쓰지 않는다.
// 6. 이모지는 ⚠️ 와 ✓ 둘뿐(DESIGN_SYSTEM.md).
//
// ── 뺀 것과 이유 (§02 REMOVE / DEFER 요약) ───────────────────────────────────
// REMOVE  회차 시간(FoldRoundTime) 표 전량 — 리포트 스스로 "발표 지표로 쓰지 말 것"
//         Median·Max 통계 전량 — 코드가 같은데 30% 흔들린다 (s3 의 두 진단값은 §02 MUST 지정분)
//         옛(로그 포함) 시리즈 수치 — 전부 과소표시본. −70.0% 한 값만 s7 에 살렸다
//         정렬 접기 벤치 결과(5,041장 시퀀스) — 폐기된 벤치. "왜 갈아엎었나" 한 줄만 s7 에
//         외부 발표 인용·발표자 실측치(12%/34%/70배) — 남의 측정
//         SRP Batcher/Dynamic Batching/GPU Instancing 비교 — 이 프로젝트의 결정이 아니다
//         bench-overlay 브랜치 절차 — "다시 쟀다"까지만 필요
// DEFER   커스텀 생명주기 프레임워크 · 외곽선 렌더링 — 둘 다 측정치가 없다(E0~E1)
//         배틀 · Geo · Motion · Surface — 근거 문서가 없어 §01 에 사실이 없다
//         기기 실측 · GPU 시간 · 오버드로우 — s10 한계로만 언급
// ─────────────────────────────────────────────────────────────────────────────

const CM_COND = 'Windows PC · Unity 에디터 PlayMode · 임의 접기 벤치(시드 12345) 16회차';

window.CM_DATA = {
  cond: CM_COND,

  /* ── S1 히어로 (Level 1) ─────────────────────────────────────────────── */
  meta: {
    eyebrow: 'PROJECT · PERFORMANCE',
    title: 'Cartapli Mobile',
    subtitle: '종이접기 시뮬레이션 — 모바일 이식과 리메이크',
    core: '성능 개선의 방향과 크기를 내 예상이 아니라 실측이 정하게 만들었다.',
    pills: [
      { text: '2026-07 ~ 08 · 진행 중' },
      { text: '커밋 122 · 단독 레포' },
      { text: 'Unity · Burst / Job' },   // sage 색면은 .cm-core 하나뿐이다 — accent 필을 쓰지 않는다
    ],
  },

  hero: {
    src: 'cartapli-mobile/assets/fold-manual.gif',
    w: 1308,
    h: 602,
    alt: '벤치 씬에서 종이를 수동으로 접는 장면. 회차를 거듭할수록 레이어가 쌓인다.',
    caption: '벤치 씬에서 손으로 접는 장면. 접는 선을 끄는 동안 매 프레임 종이 전체가 다시 만들어지고, 회차를 거듭할수록 레이어가 쌓인다. 이 페이지의 수치는 전부 이 드래그 프레임들의 비용이다.',
  },

  /* ── S2 배경 · 저작 경계 (Level 3) ───────────────────────────────────── */
  s2: {
    facts: [
      ['프로젝트', 'PC 원작 `Cartapli` 의 모바일 이식 + 리메이크. 원작과는 **별개 레포**이고 커밋 122개가 전부 단독 작업이다.'],
      ['목표', '*"DOTS 를 쓰면 빨라진다"* 가 아니라 **구조 개선으로 얼마, DOTS 로 추가 얼마**를 갈라 말하는 것. 단계마다 얼려 두고 같은 벤치로 다시 잰다.'],
      ['벤치', '정사각형 종이 한 장에 시드 12345 의 결정론 난수로 각도·비율·방향을 뽑아 **16회 접는다.** 매 단계 같은 시퀀스를 돌린다.'],
    ],
    mine: '가려진 레이어 판정 · 앞뒤 2메시 병합 렌더러 · Burst 잡 분할 파이프라인 · 오라클 테스트 · 측정 인프라. 지금 돌아가는 분할 커널도 원작 시절 내가 UV 대응으로 다시 쓴 것이고, 이 레포는 그것을 이식했다.',
    others: '점과 직선의 대칭으로 접기를 연출한다는 **아이디어와 최초 구현은 PoC 입안자의 것**이다. 원작 팀 레포의 최초 커밋에 이미 들어 있었다. 이 페이지가 다루는 것은 그 위에 올린 것들이다.',
  },

  /* ── S3 병목의 위치 (Level 2) ────────────────────────────────────────── */
  s3: {
    gist: '예상은 폴리곤 분할이었고, 실측은 렌더링 준비였다.',
    body: '회차를 거듭할수록 레이어가 늘고 한 회차에 드는 시간이 올라갔다. 자르는 계산이 무거워졌으리라 보고 폴리곤 분할부터 의심했는데, 마커를 나눠 재 보니 순서가 반대였다.',
    table: {
      headers: ['프레임당 마커', '무엇을 재는가', '회차 16 · Median'],
      rows: [
        ['`Renderer.Sync`', '레이어를 메시로 만들어 올리는 준비', '**0.636 ms**'],
        ['`FoldOperation.Split`', '접는 선으로 폴리곤을 자르는 계산', '0.083 ms'],
      ],
    },
    caption: '예상은 분할이었다. 실측에서는 렌더링 준비가 8배 비쌌다.',
    after: '정체는 기하 계산량이 아니었다. 같은 회차의 정점 총수는 세 자리인데 레이어는 수백 장이었고, 비용은 레이어 **한 장마다 붙는 유니티 객체** 쪽에 있었다. 그래서 GPU 문제로 넘기지 않고 CPU 쪽으로 방향을 잡았다 — GPU 는 가려진 픽셀을 z-buffer 로 버리지만, CPU 는 화면에 보이지도 않을 레이어의 메시까지 매번 새로 만들고 있었다.',
  },

  /* ── S4 구조 변경 (Level 2) ──────────────────────────────────────────── */
  s4: {
    gist: '레이어가 몇 장이든 렌더 오브젝트는 2개다.',
    kernel: '접기 자체 — 접는 선으로 폴리곤을 자르고 교점을 정점과 UV 에 같은 매개변수로 먹여 접힌 자리에서 텍스처가 어긋나지 않게 하는 커널 — 은 원작에서 내가 다시 쓴 것을 그대로 옮겨 왔다. 여기서 바꾼 것은 그 위다.',
    changes: [
      ['확정할 때 가려진 레이어를 데이터에서 지운다', '위 레이어부터 차례로 빼 나가면 "보이는 면적"과 "완전히 가려진 레이어"가 같은 계산에서 나온다. 두 개를 따로 만들면 같은 계산을 두 번 짓는 꼴이라 하나로 합쳤다.'],
      ['레이어당 렌더 오브젝트를 버리고 앞뒤 2메시로 병합한다', '레이어마다 두던 GameObject · MeshFilter · MeshRenderer · Mesh 를 없애고, 전 레이어의 폴리곤을 앞면·뒷면 **메시 두 개**에 이어붙인다. 쌓임 순서는 정점 z 에 굽고, 무엇이 무엇을 가리는지는 z-buffer 가 정한다. 앞뒤 구분은 두 메시의 머티리얼로만 한다.'],
    ],
    alt: '"보이는 면만 그리기"(폴리곤 불리언)는 기각했다. 정점이 세 자리뿐이라 그 계산이 아끼는 것보다 비싸다.',
    table: {
      headers: ['지표', '이전', '이후'],
      rows: [
        ['렌더 오브젝트 수', '337', '**2**'],
        ['드로우콜 델타 (정지 대조군 대비)', '+298', '**+1**'],
        ['레이어 수 (회차 16)', '337', '57'],
        ['정점 수 (회차 16)', '1,348', '251'],
      ],
    },
    reconcile: '표의 337 은 가려진 레이어를 지우기 **전** 회차 16 기준이다. 지운 뒤에는 57장이 되고, 병합한 뒤에는 장수와 무관하게 2개다. 아래 두 행은 지운 효과, 위 두 행은 병합한 효과다.',
    tradeoff: '대가도 있다. 직전 프레임과 같은 레이어를 건너뛰던 앞 단계의 최적화가 여기서 무효가 됐다 — 병합은 매 프레임 전 레이어를 다시 쌓는다. 정점이 세 자리라 실질 손실이 없다고 보고 받아들였다.',
  },

  /* ── S5 가려진 레이어 판정의 선택 (Level 3) ──────────────────────────── */
  s5: {
    intro: '가려진 레이어를 지우기로 하자, 확정할 때마다 "무엇이 가려졌나"를 판정하는 비용이 새로 생겼다. 방식이 셋 있었고 정확도도 비용도 달랐다. 고르기 전에 셋을 다 쟀다.',
    table: {
      headers: ['정책', '판정 비용', '지운 레이어 (회차 16 합)', '남는 레이어 (회차 16)', '판정'],
      rows: [
        ['① 누적 합집합·차집합 (Clipper2)', '②의 12~23배', '69장', '—', '기각 — 느린데 덜 지운다'],
        ['② 볼록 뺄셈 — 여러 장이 합쳐 덮는 경우까지', '기준', '98장', '41장', '**채택**'],
        ['③ 단일 덮개 — 한 장이 통째로 품는 경우만', '②의 1/2.7 ~ 1/2.2', '—', '57장', '기각 — 판정은 싸지만 뒤가 따라 오른다'],
      ],
    },
    decide: '③은 판정 자체가 ②보다 싸지만 덜 지운다. 남는 레이어가 41장에서 57장으로 늘면 그만큼 `Split` 과 `Renderer.Sync` 가 따라 오른다. 그래서 승패를 가른 것은 접기 **빈도**였다 — ③의 매 프레임 손해가 ②의 확정 1회 손해를 갚는 데 19프레임(60fps 에 0.32초)이 걸린다. 실제 설계는 20~30초에 한 번 접으므로 손익분기보다 60~100배 뜸하다. 분당 3회 기준 회차 16 의 프레임 평균은 ② 38.5µs, ③ 45.9µs 다.',
    tradeoff: '확정 한 프레임의 스파이크만 보면 ③이 낫다(0.247 → 0.083 ms). ②는 그 스파이크를 안고 상시 비용을 아끼는 쪽이다.',
    revert: {
      title: '되돌리는 조건',
      body: '접기 상한이 5회 이하로 확정되거나 접기 간격이 0.3초 아래로 잦아지면 ③으로 되돌린다. ③의 코드(`PaperCoverage.IsCoveredBySingle`)를 지우지 않고 남겨 뒀고, 정책 비교 테스트가 셋을 다 돈다.',
    },
  },

  /* ── S6 분할의 병렬화와 결과 순서 (Level 3) ──────────────────────────── */
  s6: {
    intro: '확정할 때만 도는 판정과 달리, 분할은 접는 선이 움직이는 **매 프레임** 돈다. 보간 프레임마다 `PaperData` · `PaperLayer` · `List<Vector2>` 를 레이어 수만큼 새로 만들고 있었다 — 프레임당 25~50KB, 400~900회 할당.',
    decision: '분할을 `NativeArray` 위 Burst 잡으로 옮기고, 결과를 관리형으로 되돌리지 않기로 했다. 확정하는 순간에만 `PaperData` 로 마샬한다. 렌더러는 항상 네이티브 뷰에서 그리므로 그리는 코드는 하나다.',
    vizCaption: '레이어마다 쓸 칸이 미리 정해져 있어 스레드끼리 구간이 겹치지 않는다. 락이 없고, **결과 순서가 스레드 속도와 무관하게 결정된다.** 쌓임 순서가 곧 종이의 앞뒤라 이 결정성은 선택이 아니다.',
    code: {
      title: 'PaperFoldSplitPipeline.cs — 잡 준비',
      intro: '기준 상태는 접기를 시작해서 확정할 때까지 고정이다. 그래서 폴드당 한 번만 올리고, 매 프레임 바꾸는 것은 접는 선 세 값뿐이다.',
      code: `PaperSplitLayersJob job = new PaperSplitLayersJob
{
    // 폴드당 한 번 채우고 그대로 재사용한다
    BaseVertices = _baseVertices, BaseUVs    = _baseUVs,
    BaseRanges   = _baseRanges,   SlotStarts = _slotStarts,
    OutVertices  = _outVertices,  OutUVs     = _outUVs,

    // 매 프레임 바뀌는 것은 이 셋뿐이다
    MidPoint = midPoint, LineNormal = lineNormal, FoldAxis = foldAxis
};
_handle = job.Schedule(_baseLayerCount, InnerLoopBatchCount);`,
      result: '버퍼는 Allocator.Persistent — TempJob 은 4프레임 제한이라 수백 프레임짜리 드래그에 못 쓴다',
    },
    compose: '쌓임 순서는 자료구조가 아니라 **순회 방향**으로 만들었다. 제자리에 남는 조각은 아래에서 위로, 넘어간 조각은 위에서 아래로 훑는다(넘어가면 순서가 뒤집히므로). 리스트를 뒤집거나 복사하는 일이 한 번도 일어나지 않는다.',
    alt: '메시 단계는 "가려낸 뒤 병렬로 처리"와 "전부 재계산" 중 앞을 골랐다. 뒤가 설령 더 빠르더라도 메모리를 더 쓰고, 그건 모바일에서 낼 수 없는 값이다. 배치 크기는 16 으로 뒀다 — 레이어당 계산이 짧아 너무 잘게 쪼개면 분배 비용이 계산보다 커진다.',
    results: [
      '`FoldOperation.Split` **0.0403 → 0.0176 ms**',
      '보간 프레임 할당 21,234B / 241회 → **7,696B / 31회**. 에디터가 정지 상태에서도 내는 바닥(17,772B / 372회)을 먼저 빼고 비교한 값이고, 독립된 두 런에서 바이트까지 같게 나왔다.',
      '`Compose` 는 0.0010 → 0.0016 ms 로 늘었다. 참조를 옮기던 일이 조각을 훑는 일로 바뀐 몫이고, 절대값이 작아 합에는 영향이 없다.',
    ],
  },

  /* ── S7 측정 자체의 신뢰 (Level 2) ───────────────────────────────────── */
  s7: {
    gist: '다섯 단계를 다 재고 난 뒤에, 그 측정이 내 성과를 줄여서 보여주고 있었다는 것을 알았다.',
    split: '`Renderer.Sync` 가 왜 비싼지 모르는 채로 범인을 짚지 않았다. 먼저 `Sync` 안을 `Fill` · `Upload` · `Log` 세 마커로 쪼갰다. 재 보니 `Fill` 과 `Upload` 를 합쳐도 `Sync` 의 10% 였다. 남은 90% 는 프레임마다 한 줄 도는 `Debug.Log` 였다 — 보간 프레임 중앙값 229,300ns 중 205,900ns, **89.8%.**',
    code: {
      title: 'PaperRenderer.cs — 로그 게이트',
      intro: '게이트를 로그 함수 안에 두면 이미 늦다. C# 은 인자를 먼저 다 계산하므로, 로그가 꺼져 있어도 보간 문자열은 만들어진다.',
      code: `// ⚠️ 매 프레임 도는 자리다
if (CLog.IsEnabled(GameLogChannel.Paper, LogLevel.Verbose))
    CLog.Verbose(GameLogChannel.Paper, $"Sync 완료 - 조각:{view.PieceCount}, …", this);`,
      result: '이 한 줄이 Renderer.Sync 비용의 89.8% 였다',
    },
    remeasure: '그 한 줄은 레이어 수와 무관하게 **모든 단계에 똑같이** 들어 있었다. 상수가 양쪽에 더해진 꼴이라 개선률이 실제보다 작게 나오고 있었다. 게이트를 고친 뒤 다섯 단계를 전부 다시 쟀다.',
    delta: {
      label: 'S0 대비 총 개선 — 프레임당 마커 합',
      before: { k: '재측정 전', v: '−70.0%' },
      after: { k: '재측정 후', v: '−93.8%' },
      note: '재측정은 내 성과를 **키웠다.** 규율이 자기에게 유리한 쪽으로만 작동한 게 아니라는 증거가 같은 사건 안에 있다.',
    },
    rules: [
      ['결론은 Average 로만 낸다', '코드가 한 줄도 바뀌지 않은 `Split` 을 노이즈 대조군으로 두고 봤더니 Average 는 ±3% 안이고 Median 과 Max 는 30% 안팎 흔들렸다. 에디터에서 Max 는 쓸 수 없는 지표다.'],
      ['자극도 의심한다', '처음 쓰던 정렬 접기 벤치는 조각이 정확히 포개지는 퇴화 패턴이라, 가려진 레이어를 지우기 시작하자 3회차에서 수렴해 끝나 버렸다. 도달할 수 없는 상한을 성과로 쓰는 꼴이라 벤치를 버리고 임의 접기로 새로 만들었다.'],
      ['같은 자극인지를 논증하지 않고 잰다', '새 벤치는 회차마다 경계상자·각도·비율을 소수점 여섯 자리로 찍는다. 세 시리즈의 CSV 를 대조해 전 회차가 일치하는 것을 확인한 뒤에 수치를 비교했다.'],
      ['내 추론도 같은 규율에 건다', '`Sync` 가 −27% 인 것을 "정점 그릇이 바뀐 덕" 이라고 적었다가, 재측정에서 −3.3%(노이즈 안)로 나와 기각하고 문서에 정정을 남겼다.'],
    ],
  },

  /* ── S8 단계별 기여의 분리 (Level 2) ─────────────────────────────────── */
  s8: {
    gist: '구조를 바꾼 몫과 Burst 잡으로 옮긴 몫을 갈라서 말할 수 있다.',
    chart: {
      unitNote: 'S0 = 100 기준',
      legend: [
        { group: 'struct', label: '구조 개선' },
        { group: 'burst', label: 'Burst 잡' },
      ],
      bars: [
        { stage: 'S0', name: '리팩터까지만', pct: 100, delta: '기준', group: 'base' },
        { stage: 'S1-1', name: '안 바뀐 레이어는 다시 만들지 않는다', pct: 58.1, delta: '−41.9%', group: 'struct' },
        { stage: 'S1-2', name: '가려진 레이어를 만들지 않는다', pct: 25.6, delta: '−74.4%', group: 'struct' },
        { stage: 'S2-b', name: '분할을 Burst 잡 + 네이티브 버퍼로', pct: 6.2, delta: '−93.8%', group: 'burst' },
      ],
    },
    caption: '프레임당 마커 합, S0 대비. 줄어든 몫의 대부분은 Burst 이전에, 구조를 바꾸는 데서 나왔다 — 0.643 → 0.040 ms.',
    note: '⚠️ S2-a(앞뒤 2메시 병합)는 이 축의 값이 이 페이지의 근거 자료에 없어 막대에서 뺐다. 그 단계에서 잰 것은 `Renderer.Sync` −81.2% 이고, 기여는 위 구조 표(렌더 오브젝트 337 → 2)로 본다.',
  },

  /* ── S9 결과 동치의 검증 (Level 4) ───────────────────────────────────── */
  s9: {
    body: '런타임을 관리형에서 네이티브·Burst 로 갈아치우면 결과가 조용히 달라질 수 있다. 쌓임 순서가 틀리면 화면에서는 색이 살짝 다른 정도로만 보여 눈으로 못 잡는다. 그래서 관리형 구현을 지우지 않고 **테스트 오라클로** 남겼다.',
    tests: [
      ['`PaperGeometryNativeTests`', '폴리곤 한 장 분할을 관리형 `PaperGeometry` 와 대조', '임의 400 케이스'],
      ['`PaperFoldSplitPipelineTests`', '파이프라인 전체를 `FoldOperation.Preview` 와 대조', '임의 200 케이스'],
      ['`PaperVisibilityTests`', '가려짐 판정을 Clipper2 기준 구현과 대조', '16 케이스'],
    ],
    why: '파이프라인 테스트가 따로 있는 이유는 폴리곤 한 장짜리 오라클로는 **쌓임 순서를 검증할 수 없기** 때문이다. 단계마다 회차별 레이어·정점 수도 대조한다 — 결정론 시퀀스라 기하 의미가 조금이라도 달라지면 숫자가 갈린다.',
  },

  /* ── S10 한계 (Level 4) ──────────────────────────────────────────────── */
  s10: [
    ['측정 환경', '전부 Windows PC · Unity 에디터 PlayMode 다. 기기 실측과 GPU 시간은 없다. 전후 비교가 성립하는 근거는 하드웨어가 아니라 모든 단계를 같은 절차로 다시 쟀다는 것이다.'],
    ['확정 프레임 할당', '그대로다(1.73MB / 23,978회). 주인은 가려짐 판정이 쓰는 Clipper2 이고 Burst 잡의 범위 밖이다. "접기의 GC 쓰레기를 없앴다" 고 말하면 거짓이 된다.'],
    ['드로우콜', '에디터에서 정지 대조군과의 **델타**로만 쟀다. development build 나 기기 실측은 없다.'],
    ['접기 빈도', '분당 2~3회는 **설계값이지 실플레이 계측이 아니다.** 판정 정책 선택이 이 값에 걸려 있다.'],
    ['판정 비용 곡선', '회차 5 · 10 · 16 세 점만 실측하고 나머지는 보간했다.'],
    ['오버드로우', '재지 않았다. 단일 덮개 정책(③)에 유리한 방향의 미확인이다.'],
    ['잔여 할당', '보간 프레임에 남은 7,696B / 31회의 정체는 규명하지 않았다. 작고 상수라 파지 않았다.'],
  ],
};
