# JCH Portfolio — 디자인 테마

> **재미를 구현의 기준으로 삼고, 구조와 데이터로 지킵니다.**
> 플레이어 경험과 시스템 구조를 먼저 보고, 만든 뒤에는 플레이와 숫자로 검증한다.
> 원본은 `pages/landing/data.js` 의 `identity` — 어긋나면 그쪽이 이긴다.

## 이 문서의 범위

**코드가 가질 수 없는 것만 적는다.** 값·스펙·구조는 전부 코드가 원본이다.

| 찾는 것 | 원본 |
|---|---|
| 색·간격·radius·폰트 크기 등 토큰 값 | `src/styles/tokens.css` |
| 카드·hover·press·애니메이션·레이아웃 | `src/styles/notebook.css` |
| 페이지 공통 골격 | `src/views/NotebookPage.jsx` |
| 표지 부품·크기 규칙 | `src/styles/cover.css` · `src/views/cover-parts.jsx` |
| 프로젝트별 표지 | `pages/{slug}/cover.jsx` |
| `data.js` 인라인 마크업 (`**굵게**` · `` `코드` `` · `*기울임*`, 중첩 금지) | `src/views/notebook-components.jsx` 의 `renderInline` |
| 페이지를 어떻게 짤지의 판단 절차 | `.claude/skills/portfolio-page/SKILL.md` |

섹션 템플릿은 이 문서도 스킬도 주지 않는다. 양식을 주면 서사가 양식에 끌려간다.

## 톤

차분하고 사실 위주. 반성적이되 자랑하지 않는다. **잘 관리된 엔지니어링 노트** — 광택 나는 SaaS 랜딩이 아니다.

여백과 1px 헤어라인이 그림자보다 많은 일을 한다. 이건 취향이 아니라 주장이다 — 구조의 명료함을 말하는 포트폴리오는 **보기에도 구조가 명료해야** 한다.

## 색

- **Sage** = 주 액센트. 활성·성공·"출시함"·개선된 수치.
- **Terracotta** = 보조. 콜아웃·문제 제기·본문 속 강조 한 곳.
- **둘 사이 그라데이션 금지.** 나란히 놓되 섞지 않는다.
- 색은 아껴 쓴다. 화면 대부분은 종이와 잉크다.
- 형광펜은 `<mark class="hl">`(sage) · `hl--terra`(terracotta) · `hl--thick`(두꺼운 획). **이 두 색 말고 다른 형광펜 색은 없다.** 정의는 `tokens.css`.

## 타이포

- Pretendard 하나로 한글·라틴 모두. 로컬 번들 (`fonts/PretendardVariable.ttf`) — CDN 아님.
- 코드·eyebrow·수치는 JetBrains Mono.
- **Light(300) 이하 금지.** 한글 글자가 차갑고 과하게 모던해져 위 톤과 정반대가 된다. 본문 400, 강조 500–600, 헤드라인 600–700.

## 글쓰기 규칙

- **수치가 말하게 한다.** "혁신적인 / 놀라운" 같은 형용사 대신 숫자를 쓴다.
- **잰 것만 성과로 쓴다.** 측정값이 없으면 **없다고 적는다** — 지어내지 않는다. 조건 없이 잰 값은 측정 방법과 재현 불가 사유를 함께 적는 자리(§ 한계)에만 둔다.
- **남의 작업은 명시한다.** 팀원·원 입안자 몫이면 ⚠️ 로 표시한다.
- 이모지 어휘는 **⚠️(주의) 와 ✓(확인됨) 둘뿐**이다. 장식용 이모지 금지.
- 문단은 짧게. 2–4문장 덩이. 목록은 명사·동사로 시작한다.
- 영문 카피는 소문자·문장부호 기본. ALL CAPS 는 mono eyebrow 라벨만 예외.
